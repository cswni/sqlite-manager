# Diagram & data

## Data

Select a table in the explorer to browse rows. If the table has a primary key, cells are editable (blur commits an `UPDATE`).

## Diagram

The **Diagram** view lays out tables with [dagre](https://github.com/dagrejs/dagre) and draws foreign-key edges. Click a node to open that table’s data view.
